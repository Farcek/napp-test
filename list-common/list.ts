import { ActionList } from './list.action'
import { FilterList } from './list.filter'
import { PagingList } from './list.paging'

export const list = {
    action: ActionList,
    filter: FilterList,
    paging: PagingList
}